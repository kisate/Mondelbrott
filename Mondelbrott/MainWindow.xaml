<Window x:Class="Mondelbrott.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*">
            </RowDefinition>
            <RowDefinition Height="32" >
            </RowDefinition>
        </Grid.RowDefinitions>
            <Canvas HorizontalAlignment="Stretch" x:Name="imgCanvas" VerticalAlignment="Stretch" Background="Blue" SizeChanged="image1_SizeChanged" Margin="2"/>
        <StatusBar Grid.Row="1">
            <Label Height="32" x:Name="lblCoordinates"/>
        </StatusBar>
    </Grid>
</Window>
